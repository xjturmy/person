# 待抓取字段清单（Lynch / Graham 评分缺口）

> 生成日期：2026-06-13
> 探针环境：理杏仁 `/api/cn/company/fs/non_financial` 端点，单股 `000333` 美的集团，2024 年度
> 探针脚本：[/tmp/probe_lx_fields.py](/tmp/probe_lx_fields.py)（v5 逐字段提交）
>
> **复核更新（2026-07-03）— 核心缺口已落地：**
> - ✅ **第三节 A（移除 7 个无效字段）已完成**：现 spec [`batch_update_fs_modules.py`](../../.tools/lixinger-archiver/batch_update_fs_modules.py) 已不含 `cce.t / stb.t / ndwoy.t / iv.t / oc.t / dnpatoshopc.t / cefppe.t / cpfdpoi.t`，仅保留有效的 `q.bs.ltl.t`（225 行）。ValidationError 整批失败链已断。
> - ✅ **第三节 A（`cce.t → mc.t`）与 B（回填货币资金/应收账款）已完成**：`q.bs.mc.t` / `q.bs.ar.t` 已加入 spec，并修复 `.config/companies.csv` 中 A 股短代码补零问题；`preson.duckdb.safety` 中非金融公司「货币资金」「应收账款」缺失均为 0。
> - 第四节覆盖快照已更新：资产/长债/流动三件套正常；货币资金 81 家、应收账款 81 家，覆盖所有非金融公司。
> - 第三节 C（衍生函数接线）未复核，维持待办。
> 下方为原始清单，保留备用。

---

## 一、关键背景

- **生产 spec**：[batch_update_fs_modules.py:210-248](batch_update_fs_modules.py#L210-L248)
- **触发原因**：Lynch L2（净现金/市值）、Graham G3（NCAV 净流动资产）需要 BS 明细科目（货币资金 / 短期借款 / 存货 等）。其中货币资金/应收账款已补齐，短期借款/一年内到期仍需替代源。
- **静默失败链**：spec 里同时包含多个非法字段时，理杏仁返回 `code=0 ValidationError`，**整批抓取失败**；当前生产 spec 至少有 6 个非法字段共存。

---

## 二、字段可用性矩阵（逐字段实测）

### ✅ 可用 — 立即可抓

| 理杏仁字段 | 中文名 | 用途 | 引擎位置 |
|------------|--------|------|----------|
| `q.bs.mc.t` | 货币资金 | Lynch L2 净现金 / Graham G3 NCAV | classifier.py:1336 |
| `q.bs.ar.t` | 应收账款 | turnover 派生（已有备用源） | extras.py |
| `q.bs.ltl.t` | 长期负债合计 | Graham 防御 7 准则 | steps.py:283 |
| `q.ps.tp.t` | 营业利润 | interest_coverage 反推 | derived_metrics.py:439 |

> `q.bs.mc.t` / `q.bs.ar.t` 已加入生产 spec；补零后重跑 16 家短代码公司，非金融覆盖已清零缺失。

---

### ❌ 不可用 — 需替代方案

| 理杏仁字段（生产 spec 中） | 中文名 | 影响维度 | 替代方案 |
|----------------------------|--------|----------|----------|
| `q.bs.cce.t` | 货币资金（别名） | Lynch L2 | **改用 `q.bs.mc.t`** |
| `q.bs.stb.t` | 短期借款 | Lynch L2 净现金扣减项 | Tushare / akshare BS 表 |
| `q.bs.ndwoy.t` | 一年内到期非流动负债 | Lynch L2 净现金扣减项 | Tushare / akshare BS 表 |
| `q.bs.iv.t` | 存货 | turnover.duckdb（已有 sina 派生） | 已落地 |
| `q.is.oc.t` | 营业成本 | 毛利率/营运效率 | `q.m.gp_m.ttm` 间接（毛利率已有）|
| `q.ps.dnpatoshopc.t` | 扣非净利润 | 林奇成长质量 | `q.m.np_s_r.ttm` 间接 |
| `q.cfs.cefppe.t` | 资本开支 | FCF 健康度 | FCF 已抓（间接） |
| `q.cfs.cpfdpoi.t` | 利息支出 | interest_coverage 真分母 | 财务费用率反推（已实现） |

---

## 三、修复动作清单

### A. spec 修正（影响所有抓取）

[batch_update_fs_modules.py:210-248](batch_update_fs_modules.py#L210-L248) **必须修**：

```python
# 244 行原值（❌ 整批 ValidationError 元凶）
("05_安全性分析", "货币资金", "q.bs.cce.t", None, "货币资金", None),
# 改为
("05_安全性分析", "货币资金", "q.bs.mc.t", None, "货币资金", None),

# 移除以下 7 行（❌ 字段无效，导致整批抓取失败）
"q.bs.stb.t"  / "q.bs.ndwoy.t"  / "q.bs.iv.t"
"q.is.oc.t"   / "q.ps.dnpatoshopc.t"
"q.cfs.cefppe.t"  / "q.cfs.cpfdpoi.t"
```

### B. 回填 100 家（spec 修完后）

```bash
cd /Users/gongyong/Desktop/Keyi/preson && source .venv/bin/activate

# 单家试通（美的）
python3 .tools/lixinger-archiver/batch_update_fs_modules.py \
  --companies-csv .config/companies.csv \
  --base-dir 02_companies \
  --only 000333 \
  --years 10

# 全量回填
python3 .tools/lixinger-archiver/batch_update_fs_modules.py \
  --companies-csv .config/companies.csv \
  --base-dir 02_companies \
  --years 10

# 入库
python3 .tools/db/ingest_consolidated.py
```

### C. 衍生函数接线

- `cash_minus_st_debt_to_mc` ([derived_metrics.py:377](.tools/dashboard/valuation/derived_metrics.py#L377))：
  - 当前只查 `市值(港币)`，**漏 A 股** → 加 `市值(元)` fallback
  - 短期借款 / 一年内到期既然无源，**改算简化版 cash_to_mc**（货币资金/总市值），note 标 "未扣短债"
- Lynch classifier.py:1336 调用 `cash_minus_st_debt_to_mc` 接线
- Graham steps.py NCAV 用同一函数

---

## 四、当前 safety 表 BS 覆盖快照

```
                 字段  覆盖公司数  总数
       资产合计           82      100
       长期负债合计        82      100
       流动资产合计        81      100
       流动负债合计        81      100
       货币资金            81     100   ← 覆盖所有非金融公司
       短期借款            0      100   ← 无源
       一年内到期非流动负债   0      100   ← 无源
       存货                0      100   ← 转入 turnover.duckdb（sina 派生）
       应收账款            81     100   ← 覆盖所有非金融公司
```

金融/港股口径不纳入非金融 BS 明细硬性覆盖；短期借款、一年内到期非流动负债仍需其它源。

---

## 五、超出理杏仁能力的字段（需其它源）

| 字段 | 引擎需求 | 推荐源 |
|------|----------|--------|
| 短期借款 / 一年内到期 | Lynch L2 净现金 | Tushare `fina_balance_sheet`（已订阅）|
| 内部人净买入 (L2) | Lynch 反向指标 | akshare `stock_zt_pool_em` 派生 |
| 机构持仓变化 (L3) | Lynch 机构信号 | akshare（已实现 fallback）|
| NPL / CET1 | 银行业 Piotroski | sina BS + 央行口径 |
| EV·NBV | 保险业 Graham | 年报手工录入 |

---

## 六、优先级建议

| 优先级 | 动作 | 解锁能力 |
|--------|------|----------|
| **P0** | spec 244 行 `cce.t → mc.t` + 移除 7 行无效字段 | ✅ 已完成 |
| **P0** | 全量回填货币资金/应收账款 | ✅ 非金融公司缺失为 0 |
| **P1** | 衍生函数 `市值(元)` A 股 fallback + 简化 cash_to_mc | classifier 接线 |
| **P2** | Tushare 接入短期借款 / 一年内到期 | Lynch L2 完整版（net cash） |
| **P3** | 排查 18 家 BS 三件套缺失公司 | safety 表全 100 家覆盖 |

---

📌 **同步更新**：本探针发现需写回 [reference_lixinger_field_dict.md](~/.claude/projects/-Users-gongyong-Desktop-Keyi-preson/memory/reference_lixinger_field_dict.md) — 把以上 8 个 ❌ 字段加入黑名单，把 `q.bs.mc.t / q.bs.ar.t / q.ps.tp.t` 加入白名单。
