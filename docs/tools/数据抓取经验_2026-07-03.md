# 数据抓取经验记录（2026-07-03）

> 本文沉淀 2026-07-03 全模块缺失排查后的脚本修复、数据源兜底和验收口径。长期抓数入口见 [抓数层](../architecture/03-fetch-layer.md)，字段级白/黑名单见 [待抓取字段清单](./待抓取字段清单.md)。

## 本轮结论

| 模块 | 修复 | 验收结果 |
|---|---|---|
| 理杏仁财务模块 | 新增 `q.bs.mc.t` 货币资金、`q.bs.ar.t` 应收账款；读取公司清单时统一 A 股 6 位/港股 5 位补零 | 非金融公司 `货币资金` 缺失 0 家、`应收账款` 缺失 0 家 |
| 公司 K 线 | 港股改用 AkShare `stock_hk_hist`；缓存只差当天时默认跳过 0 行探测 | `prices` 覆盖 100 ticker，最新 2026-07-03 |
| 市场快照 | EM 全 A 快照失败时切新浪；行业字段用本地公司清单/行业缓存兜底 | 写入 5533 ticker，自选池行业命中 99 条 |
| 同行库 | `--skip-fundamentals` 时保留 `.config/peers.csv` 旧基本面字段 | 28 行 peer，`peer_roe` 非空 28 |
| 黄金 ETF 份额 | 默认跳过不稳定 AkShare 份额接口，使用手填 CSV 或成交量代理 | `gold_etf_share` 覆盖 4 ETF / 2932 行 |
| 预计算 | 主库和专项库刷新后重跑 `precompute.py` | `screener_wide` 100 行，`company_bundle` 100/100 |
| 林奇增长连续性 | 港股/新股半年报、年报按覆盖季度数计权,不按披露条数硬套 8 季铁律 | 蜜雪集团有效覆盖季度按 5/8 处理,样本不足只给观察分 |

## 根因

1. **短代码导致静默空数据**：`.config/companies.csv` 中部分 A 股是 `333`、`858`、`2475` 这类短代码。理杏仁 API 要求 6 位 A 股代码，短代码请求可能不报错但返回空表。
2. **BS 明细字段缺失**：Lynch/Graham 需要货币资金、应收账款等资产负债表科目，原财务模块没有正式抓 `q.bs.mc.t` / `q.bs.ar.t`。
3. **外部接口不稳定**：东方财富全 A 快照、行业映射、AkShare 部分接口会断连或返回空，需要脚本内置兜底。
4. **缓存刷新会误伤旧值**：同行脚本跳过基本面抓取时，如果直接写空字段，会覆盖 `.config/peers.csv` 的旧可用快照。
5. **披露频率不等于季度样本**：港股/新上市公司常只有半年报和年报。若把 H1/年报当成单个“季度”或把年报累计值当单季值,会误判林奇增长连续性。

## 固化到脚本的规则

| 脚本 | 经验 |
|---|---|
| `.tools/lixinger-archiver/batch_update_fs_modules.py` | 读公司清单时统一补零；非金融 spec 加入 `q.bs.mc.t` / `q.bs.ar.t`；支持 `--only-folder` 定点补抓 |
| `.tools/db/fetch_akshare.py` | 港股用 `stock_hk_hist`；默认跳过只差当天的行情探测，强刷用 `--allow-today-probe` |
| `.tools/db/fetch_market_spot.py` | EM/Sina 行情和行业映射分层兜底；自选池行业映射优先本地缓存 |
| `.tools/db/fetch_peers.py` | 缓存模式保留旧 peer 基本面，避免 ROE/PEG 被空值覆盖 |
| `.tools/db/fetch_gold_etf_share.py` | AkShare 份额接口默认不跑；优先手填 CSV，否则用成交量代理 |
| `.tools/db/update.py` | 黄金范式/过热快照路径指向现有 `dashboard/gold/*.py` |

## 林奇增长连续性派生原则

林奇“8 季增长连续性”只能在披露频率足够完整时按单季 YoY 直接判断。遇到港股、新上市公司或只披露半年报/年报的公司,按以下规则处理:

1. **披露记录数不等于季度数**
   - 一条 H1 半年报覆盖 Q1-Q2,应按 2 个季度计权。
   - 一条年报若能用全年累计 - 半年累计拆出 H2,应按 Q3-Q4 两个季度计权。
   - 单季报才按 1 个季度计权。

2. **累计值不能强行当单季值**
   - Q2/Q3/Q4 只有在同年上一期累计值存在时,才可做“当期累计 - 上期累计”。
   - 若缺上一期累计值,该期只能作为覆盖期数据或跳过,不能用 0 补上一期。

3. **有效覆盖季度 < 6/8 时只做观察**
   - 不判定“快速增长铁律达标”。
   - 也不判定“增长属性丧失”。
   - ABCD 评分按已有覆盖季度的命中率给临时分,并封顶,避免 2 个样本就给满分。

4. **评分示例**
   - 快速增长型:2/2 覆盖季度均 >20%,且最新仍强,可给高观察分,但不满分。
   - 1/2 >20%,给中等观察分。
   - 0/2 但最新为正,给低观察分。
   - 0/2 且最新为负,给低分。

5. **页面展示**
   - 写“有效覆盖季度 5/8”,不要写“近 5 季”。
   - 使用半年报/年报派生时,标注“覆盖期 YoY / 样本不足”。
   - 蜜雪集团这类港股的增长连续性应解释为“披露样本不足但覆盖期增速较强/较弱”,不能机械套 8 季红绿灯。

## 验收命令

```bash
.venv/bin/python - <<'PY'
import duckdb

con = duckdb.connect('data/preson.duckdb', read_only=True)
print('prices', con.execute("SELECT COUNT(DISTINCT ticker), COUNT(*), MAX(date) FROM prices").fetchone())
for metric in ['货币资金', '应收账款']:
    missing = con.execute("""
      SELECT COUNT(*) FROM companies c
      WHERE c.category='non_financial'
        AND NOT EXISTS (
          SELECT 1 FROM safety s
          WHERE s.ticker=c.ticker AND s.metric=? AND s.value IS NOT NULL
        )
    """, [metric]).fetchone()[0]
    print(metric, 'missing_non_financial=', missing)
con.close()

con = duckdb.connect('data/analytics.duckdb', read_only=True)
print('analytics', con.execute("SELECT (SELECT COUNT(*) FROM screener_wide), (SELECT COUNT(*) FROM company_bundle)").fetchone())
con.close()
PY
```

2026-07-03 基准结果：

```text
prices: 100 ticker / 229593 行 / 最新 2026-07-03
货币资金 missing_non_financial=0
应收账款 missing_non_financial=0
analytics: 100 / 100
```

## 后续仍需留意

| 项 | 影响 | 建议 |
|---|---|---|
| 同行库覆盖偏窄 | 公司研究同行对标只覆盖历史备份中的 14 家 self / 28 条 peer | 后续换源或扩充 `.config/peers.csv` |
| 全市场行业在线映射不稳 | 市场扫描的全市场行业筛选受限 | 自选池已可用；全市场行业筛选需单独引入稳定行业表 |
| 黄金 ETF 份额为代理 | 过热信号第 5 项不是基金真实份额 | 找到稳定份额源后写入 `.config/gold_etf_share_manual.csv` 或替换抓取源 |
| 短期借款/一年内到期负债无理杏仁源 | Lynch L2 完整净现金仍不能精算 | 后续接 Tushare/AkShare 资产负债表 |
