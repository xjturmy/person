# preson Dashboard 依赖(锁定版本,2026-05-02)
# 安装: .venv/bin/pip install -r .tools/dashboard/requirements.txt

streamlit==1.57.0
plotly==6.7.0
pandas==3.0.2
duckdb==1.5.2
pyarrow==24.0.0

# 候选 ⑩(v2.4 step-B)全局搜索栏 — 拼音首字母 + 模糊兜底
pypinyin==0.55.0
rapidfuzz==3.14.5

# Streamlit 间接依赖(锁定避免 itsdangerous/jinja2 漂移)
altair==6.1.0
click==8.3.3
narwhals==2.20.0
protobuf==7.34.1
pydeck==0.9.2
tenacity==9.1.4
GitPython==3.1.49
